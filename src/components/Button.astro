---
const { url = "", newtab = false, As = "a", ...props } = Astro.props;
---

<As
	class={`not-prose button-peru`}
	href={url}
	target={newtab ? "_blank" : "_self"}
	rel={newtab ? "noopener noreferrer" : ""}
	{...props}
>
	<slot />
</As>
